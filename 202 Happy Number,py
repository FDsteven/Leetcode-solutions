def isHappy(n):
    """
    :type n: int
    :rtype: bool
    """

    seen = set()
    while n > 1 and n not in seen:
        seen.add(n)
        digits = [int(d) for d in str(n)]
        n = sum([item ** 2 for item in digits])
        
    return n == 1

n = 19
n = 2
print(isHappy(n))